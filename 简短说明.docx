这个课程资料整理器体现了 Python 的以下作用：
1. 快速编写自动化脚本
   用几十行代码就能实现文件的扫描、分类、复制和报告生成，替代了手动整理文件夹的重复劳动。
2. 适合处理文件、路径和批量任务
   `pathlib` 模块让路径操作（读文件名、判断后缀、创建目录等）变得简单可靠，批量处理文件像操作列表一样方便。
3. 用字典、函数、模块把规则组织清楚
   分类规则（后缀→目录）用字典存储，清晰易维护。  
   核心逻辑拆成独立函数（扫描、计划、执行、报告），模块化结构便于分工和复用。
4. 通过命令行参数让程序更灵活
   `argparse` 解析 `--source`、`--target`、`--dry-run`、`--mode` 等参数，让同一个程序适应不同使用场景，无需修改代码。